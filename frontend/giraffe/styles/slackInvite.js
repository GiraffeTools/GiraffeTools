import componentStyles from "./components";

const styles = {
  slackInfo: {
    color: "grey",
    marginTop: "2rem",
    marginBottom: "6rem",
    fontSize: "1.4rem",
    textAlign: "center"
  },
  join: {},
  separator: {
    ...componentStyles.separator
  }
};

export default styles;

import styleSheet from "../constants/styles";

const styles = {
  thanks: {
    textAlign: "center",
    backgroundColor: styleSheet.primaryColor,
    padding: "2rem 0rem"
  },

  thanksLogo: {
    width: "20%",
    margin: "2rem"
  }
};

export default styles;

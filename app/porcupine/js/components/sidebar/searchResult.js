import React from "react";
import Radium from "radium";

import styles from "../../styles/searchResult.js";

const SearchResult = ({ result }) => <div style={[styles.result]} />;

export default Radium(SearchResult);
